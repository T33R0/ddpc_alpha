```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Use a separate Supabase project for local dev to avoid cross-logout with prod
SUPABASE_SERVICE_ROLE=  # server-only if ever used
NEXT_PUBLIC_E2E_AUTH_BYPASS=0
```
